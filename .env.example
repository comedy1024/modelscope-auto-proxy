# ModelScope API 配置（注意：需要包含 ms- 前缀）
MODELSCOPE_API_KEY=ms-your_api_key_here
# 代理服务监听端口
PROXY_PORT=8000
# 代理服务对外暴露的模型名称（客户端用这个名字调用）
VIRTUAL_MODEL_NAME=modelscope-auto
# 模型参数量下限（B），只获取大于此值的文本模型
MIN_PARAM_B=4
# 模型刷新时间（秒），默认每天刷新一次
MODEL_REFRESH_INTERVAL=86400
# 日志级别
LOG_LEVEL=INFO
